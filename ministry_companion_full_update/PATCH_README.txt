Drop this folder's **src/App.jsx** into your existing Vite project (replace the file).
Then:
  git add src/App.jsx
  git commit -m "Patch: editable addresses+warning, NH timestamps, predictive scripture tool, labeled lesson"
  git push origin main
Netlify will rebuild automatically.
